import React from "react";

const Navbar = () => {
  return (
    <nav
      style={{
        width: "100%",
        padding: "1rem",
        background: "#222",
        color: "#fff",
        textAlign: "center",
      }}
    >
      <h1>Navbar</h1>
    </nav>
  );
};

export default Navbar;
